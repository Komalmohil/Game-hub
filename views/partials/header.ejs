<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
   <link rel="stylesheet" href="/css/header.css">
</head>
<body>
    <header class="main-header">
  <h2 class="logo"><a href="/">GameHub</a></h2>
  <nav class="nav-links">
    <% if (user) { %>
      <span class="welcome-text">Welcome, <%= user.username %></span>
      <% if (typeof game !== 'undefined' && game) { %>
    <% if (typeof onLeaderboard !== 'undefined' && onLeaderboard) { %>
      
      <a href="/<%= game.toLowerCase().replace(/ /g, '-') %>" class="nav-btn toggle-btn">Return to Game</a>
    <% } else { %>
    
      <a href="/leaderboard/<%= game.toLowerCase().replace(/ /g, '-') %>" class="nav-btn toggle-btn">Leaderboard</a>
    <% } %>
  <% } %>

      <a href="/logout" class="nav-btn logout">Logout</a>
    <% } else { %>
      <a href="/login" class="nav-btn">Login</a>
      <a href="/signup" class="nav-btn signup">Signup</a>
    <% } %>
  </nav>
</header>

</body>
